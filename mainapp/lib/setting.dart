import 'package:flutter/material.dart';

// ignore: camel_case_types
class settingPage extends StatelessWidget {
  const settingPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('settingwindow'),
      ),
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
